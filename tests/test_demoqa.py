from driver.driver import Driver
from pages.interactions import Interactions
from pages.elements import Elements
from pages.widgets import Widgets

# def test_open_interactions_selectable(browser):
#     browser.get(Driver.URL_demoqa)
#
#     Interactions().scroll_down(browser)
#     Interactions().click_dropdown_interactions(browser)
#     Interactions().click_selectable(browser)
#     Interactions().scroll_up(browser)
#     Interactions().click_selectable_list_element(browser)


#
# def test_open_widgets_autocomplete(browser):
#     browser.get(Driver.URL_demoqa)
#
#     Widgets().scroll_down(browser)
#     Widgets().click_dropdown_widgets(browser)
#     Widgets().click_autocomplete(browser)
#     Widgets().scroll_up(browser)
#     Widgets().select_and_send(browser, "pur")
